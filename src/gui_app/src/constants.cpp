#include "constants.h"

namespace constants {
    const int BLOCK_BORDER_RADIUS = 20;

    const int BLOCK_BORDER_WIDTH = 2;

    // dark gray
    const SkColor WINDOW_BACKGROUND_COLOR = SkColorSetARGB(0xFF, 0x40, 0x40, 0x40);

    // dark blue
    const SkColor BLOCK_BORDER_COLOR = SkColorSetARGB(0xFF, 0x00, 0x00, 0x80);

    const SkColor BLOCK_BACKGROUND_COLOR = SkColorSetARGB(0xFF, 0x00, 0x00, 0x80);

    // white
    const SkColor BLOCK_TEXT_COLOR = SkColorSetARGB(0xFF, 0xFF, 0xFF, 0xFF);

    const int BLOCK_TEXT_SIZE = 20;

}  // namespace constants
